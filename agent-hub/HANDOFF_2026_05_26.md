# RaddFlix — Agent Handoff Document
> **Last updated:** 2026-05-26 Session 5  
> **READ THIS FIRST** — everything a new agent needs to start without wasting tokens

---

## 0. START HERE — Zero-Waste Quickstart

```bash
# Step 1: Decode SSH key (env var has embedded spaces — strip them first)
python3 -c "
import os, base64
raw = os.environ['ORACLE_SSH_KEY']
# Key may have spaces instead of newlines — reconstruct properly
raw = raw.strip()
if ' ' in raw:
    header = '-----BEGIN OPENSSH PRIVATE KEY-----'
    footer = '-----END OPENSSH PRIVATE KEY-----'
    body = raw.replace(header, '').replace(footer, '').strip()
    body_clean = '\n'.join([body[i:i+64] for i in range(0, len(body.replace(' ','')), 64)])
    key = header + '\n' + body_clean + '\n' + footer + '\n'
else:
    key = base64.b64decode(raw).decode()
with open('/tmp/oracle_key','w') as f: f.write(key)
import subprocess; subprocess.run(['chmod','600','/tmp/oracle_key'])
print('Key written to /tmp/oracle_key')
"

# Step 2: Test connections
ssh -i /tmp/oracle_key -o StrictHostKeyChecking=no ubuntu@92.4.95.252 "echo Oracle OK && sudo supervisorctl status"
curl -s -H "Authorization: token $GITHUB_TOKEN" https://api.github.com/repos/raddclub/raddflix-app | python3 -c "import sys,json; d=json.load(sys.stdin); print('GitHub OK:', d.get('full_name'), '| private:', d.get('private'))"

# Step 3: Read TASK_LOG to see what was done
curl -sL -H "Authorization: token $GITHUB_TOKEN" \
  "https://api.github.com/repos/raddclub/raddflix-app/contents/agent-hub/history/TASK_LOG.md" | \
  python3 -c "import sys,json,base64; j=json.load(sys.stdin); print(base64.b64decode(j['content']).decode())" | tail -80
```

---

## 1. Project Identity

| Field | Value |
|---|---|
| App name | **RaddFlix** (final — never change) |
| Package ID | `com.raddflix.app` |
| GitHub repo | `raddclub/raddflix-app` (PUBLIC — unlimited free CI minutes) |
| Oracle server | `ubuntu@92.4.95.252` |
| Server root | `/opt/jazzmax/` (internal name — DO NOT rename) |
| SSH key | `$ORACLE_SSH_KEY` env var (see quickstart above for decode) |
| GitHub token | `$GITHUB_TOKEN` env var |
| Brand color | `#E8002D` |
| Tagline | "Pakistan ka entertainment, data-free" |

**Important naming history:** App was renamed JazzMAX → Zeno → RaddFlix.  
Old names appear only in server folder paths (harmless, intentional). Never introduce them into user-visible code.

---

## 2. GitHub API Rules (CRITICAL)

**The repo is public.** Use raw GitHub for reading, API for writing.

```python
import urllib.request, base64, json, os
TOKEN = os.environ['GITHUB_TOKEN']

# READ a file
def gh_read(path):
    url = f"https://api.github.com/repos/raddclub/raddflix-app/contents/{path}"
    req = urllib.request.Request(url, headers={"Authorization": f"token {TOKEN}"})
    j = json.loads(urllib.request.urlopen(req).read())
    return base64.b64decode(j["content"]).decode("utf-8"), j["sha"]

# WRITE a file  
def gh_write(path, content, sha, message):
    url = f"https://api.github.com/repos/raddclub/raddflix-app/contents/{path}"
    payload = json.dumps({
        "message": message,
        "content": base64.b64encode(content.encode()).decode("ascii"),
        "sha": sha
    }).encode()
    req = urllib.request.Request(url, data=payload, method="PUT",
        headers={"Authorization": f"token {TOKEN}", "Content-Type": "application/json"})
    r = json.loads(urllib.request.urlopen(req).read())
    return r["commit"]["sha"]
```

- **Always fetch current SHA** before writing (GitHub rejects writes with wrong SHA)  
- **Never use `git` shell commands** — main agent cannot push directly  
- **Never use `raw.githubusercontent.com`** for private paths (repo was private until Session 5)

---

## 3. Server Architecture

### Services (managed by Supervisor)
| Name | Supervisor ID | Port | Entry file |
|---|---|---|---|
| Admin backend (Radd Hub) | `jazzmax_radd` | 5000 | `/opt/jazzmax/radd-hub/radd_hub.py` |
| Mobile API | `jazzmax_watch` | 6000 | `/opt/jazzmax/_watch_prototype/run.py` |

```bash
sudo supervisorctl status           # check
sudo supervisorctl restart jazzmax_watch jazzmax_radd   # restart both
sudo supervisorctl tail -f jazzmax_watch   # live logs
```

### Database
- **SQLite** at `/opt/jazzmax/radd-hub/data/raddflix.db`
- 69 titles, ~12 with actual JazzDrive video files
- 8 registered users, 1 paid subscriber

### Critical path — NEVER delete
`/opt/jazzmax/radd-hub/hub/_legacy/` — `jazzdrive.py` and `scanner.py` import from it

### Remote config (change API URL without APK rebuild)
`raddflix_config.json` at repo root → app fetches on every launch:
```json
{ "api_base_url": "http://92.4.95.252" }
```

---

## 4. GitHub Actions (CI/CD)

**Repo is PUBLIC** → unlimited free minutes on `ubuntu-latest`. No billing issues.

| Workflow | File | Trigger | Purpose |
|---|---|---|---|
| Build RaddFlix APK | `build-apk.yml` | push to main + manual | Builds signed APK |
| RaddFlix CI | `ci-tests.yml` | push to main | Flutter analyze + API health check |

**LEGACY workflow** `.github/workflows/build_apk.yml` (underscore) — DELETE IT. It uses wrong paths and old Flutter version.

### APK signing secrets (set in GitHub repo Settings → Secrets)
| Secret | Value |
|---|---|
| `KEYSTORE_BASE64` | base64 of `raddflix_flutter/android/app/keystore/raddflix.keystore` |
| `KEYSTORE_PASSWORD` | keystore password |
| `KEY_ALIAS` | `raddflix` |
| `KEY_PASSWORD` | key password |

If `KEYSTORE_BASE64` is not set, the build auto-generates a keystore and prints its base64 to the build log.

### Trigger a manual build
```bash
curl -s -X POST -H "Authorization: token $GITHUB_TOKEN" \
  -H "Content-Type: application/json" \
  "https://api.github.com/repos/raddclub/raddflix-app/actions/workflows/build-apk.yml/dispatches" \
  -d '{"ref":"main"}'
```

### Check build status
```bash
curl -sL -H "Authorization: token $GITHUB_TOKEN" \
  "https://api.github.com/repos/raddclub/raddflix-app/actions/runs?per_page=5" | \
  python3 -c "import sys,json; [print(r['id'], r['status'], r.get('conclusion','-'), r['name'][:25], r['created_at'][11:19], 'sha:', r['head_sha'][:8]) for r in json.load(sys.stdin)['workflow_runs']]"
```

### Diagnose a failed build (get step-by-step)
```bash
# Replace RUN_ID with the actual run ID
curl -sL -H "Authorization: token $GITHUB_TOKEN" \
  "https://api.github.com/repos/raddclub/raddflix-app/actions/runs/RUN_ID/jobs" | \
  python3 -c "import sys,json; [print(j['name'], j['conclusion'], [(s['name'],s['conclusion']) for s in j.get('steps',[])]) for j in json.load(sys.stdin)['jobs']]"
```

**If builds fail with runner_id: 0 and complete in 2 seconds:**  
This means GitHub Actions minutes are exhausted. Since the repo is now public this should not happen. If it does, the repo may have been made private again.

---

## 5. Flutter App Structure

### Active app folder: `raddflix_flutter/` (NOT root `lib/` — that's old prototype)

### Key files
```
raddflix_flutter/lib/
├── main.dart                        # entry: RemoteConfig → AppUpdateService → runApp
├── app.dart                         # MaterialApp, all routes, ForceUpdateGuard
├── core/
│   ├── constants.dart               # AppConstants, AppColors, AppRoutes, StorageKeys
│   │   └── jazzDriveDbUpdateUrl     # ← SET to 'http://92.4.95.252/api/catalog/db_update'
│   ├── remote_config.dart           # fetches api_base_url from GitHub raddflix_config.json
│   ├── api/api_client.dart          # Dio + automatic JWT refresh interceptor
│   ├── db/
│   │   ├── local_db.dart            # SQLite schema v10
│   │   └── sync_service.dart        # catalog sync + JazzDrive zero-rated fallback
│   └── services/
│       ├── jazzdrive_service.dart   # ⭐ zero-rated stream link generation (KEY FILE)
│       ├── poster_service.dart      # TMDB→JazzDrive poster chain
│       └── app_update_service.dart  # force update check
├── providers/
│   ├── auth_provider.dart           # JWT lifecycle, token refresh
│   └── catalog_provider.dart        # catalog state + sync orchestration
├── models/catalog_item.dart         # CatalogItem model
└── screens/
    ├── splash_screen.dart           # ✅ RaddFlix branded
    ├── home_screen.dart             # ✅ Netflix-style hero + rows
    ├── player_screen.dart           # ✅ media_kit, PiP, Chromecast
    ├── show_detail_screen.dart      # ✅ episodes, seasons
    ├── search_screen.dart           # ⚠️ basic shell (Phase 4 incomplete)
    ├── downloads_screen.dart        # ⚠️ basic shell (Phase 5 incomplete)
    ├── profile_screen.dart          # ⚠️ basic shell (Phase 6 incomplete)
    └── subscription_screen.dart    # ⚠️ basic shell (Phase 8 incomplete)
```

### Working features ✅
- Auth (phone OTP + email + guest), JWT refresh, device binding
- Home screen (hero banner, Continue Watching, Trending, Free rows, category chips)
- Video player (media_kit, MKV, EAC3, multi-audio, PiP, Chromecast)
- Zero-rated stream links (JazzDrive 2-step API, 6hr cache)
- Catalog sync from Oracle + JazzDrive fallback
- Vault (biometric/PIN protected private content)
- Watch progress saved to SQLite

### Not done yet ⚠️
- Phase 3: Player gestures (double-tap seek, swipe volume/brightness, speed, subtitles)
- Phase 4: Search screen (filters, history, live results)
- Phase 5: Downloads screen (folder UI, progress, delete)
- Phase 6: Profile screen (stats, theme toggle)
- Phase 8: Subscription flow (TID submission, status)

---

## 6. JazzDrive Rules (NEVER break)

```
✅ JazzDrive = VIDEO links ONLY
❌ NEVER use JazzDrive for images (posters, thumbnails, banners)
✅ Images: TMDB API → IMDB → OMDB → Google → YouTube

Link generation (jazzdrive_service.dart):
  Step 1: POST https://cloud.jazzdrive.com.pk/sapi/link/login?action=login
          Body: { "shared": true, "key": shareKey }
          → returns { validationkey, Set-Cookie: JSESSIONID }
  Step 2: GET  https://cloud.jazzdrive.com.pk/sapi/media/video?action=get
               &shared=true&key=shareKey&validationkey=VK
          Header: Cookie: JSESSIONID=...
          → returns record with { url, name }
  Step 3: Final URL = record.url + "?filename=" + encodeURIComponent(record.name)
  
  ⚠️  NEVER append validationkey= to final URL — k= token is self-signing
  shareKey regex: /\/(?:share-landing\/f|share\/f|f)\/([^\/?#]+)/
  Cache: 6 hours in memory + stream_cache SQLite table
```

---

## 7. Oracle Server API Endpoints

Base URL: `http://92.4.95.252`  
Watch API (port 6000, routed via Nginx): all `/watch/api/` and `/api/` paths

### Key endpoints
| Method | Path | Purpose |
|---|---|---|
| POST | `/api/auth/register` | Register |
| POST | `/api/auth/login` | Login |
| POST | `/api/auth/guest` | Guest token |
| POST | `/api/auth/refresh` | Refresh JWT |
| GET | `/api/auth/me` | User info |
| GET | `/api/catalog/version` | DB version check |
| GET | `/api/catalog/sync` | Full catalog sync |
| GET | `/api/catalog/db_update` | JazzDrive zero-rated catalog JSON |
| GET | `/watch/api/play/{file_id}` | Get stream URL |
| GET | `/api/subscription/plans` | Plan list |
| POST | `/api/subscription/tid/submit` | Submit payment TID |
| GET | `/api/subscription/status` | Check sub status |
| GET | `/api/notifications/` | Notifications |
| GET | `/api/payment-methods` | Payment methods |
| GET | `/api/history` | Watch history |

### `/api/catalog/db_update` response format
```json
{
  "version": 1779705973,
  "titles": [ { "id", "title", "media_type", "year", "genres", "rating",
                "runtime", "language", "description", "poster_url",
                "share_url", "is_free": 0, "file_id", "db_version" } ],
  "episodes": [ { "id", "title_id", "season", "episode", "title",
                  "share_url", "is_free": 0, "file_id", "db_version" } ]
}
```
`is_free` is `int` (0 or 1), NOT Python bool — was fixed in Session 4 (BUG-001b).

---

## 8. Session History Summary

| Session | Date | Key Work |
|---|---|---|
| 1 | 2026-05-26 | Full JazzMAX→RaddFlix rebrand (80 files), built agent-hub system |
| 2 | 2026-05-26 | APK build fixed (workflow paths, proguard, branding), BUG-001 through BUG-012 server fixes |
| 3 | 2026-05-26 | show_detail_screen Dart syntax fix (BUG-005), successful APK build `RaddFlix-1.0.0+1-build237.apk` |
| 4 | 2026-05-26 | BUG-001b `is_free` int fix on Oracle, JazzDrive sync URL in constants.dart |
| 5 | 2026-05-26 | JazzDrive sync wired (`jazzDriveDbUpdateUrl` set), builds failing (minutes exhausted), repo made PUBLIC, builds restored |

**Latest successful APK:** `RaddFlix-1.0.0+1-build237.apk` (46.62 MB, run 26472137136, commit d0d3b9c9)

---

---

## 12. NEXT TASK — Supreme Player Implementation

**Full spec:** `agent-hub/PLAYER_SPEC.md` — read this entire file before writing any code.

### What to build (in priority order)

| Phase | Work | Effort |
|---|---|---|
| 3A | `PlayerPrefs` model + provider, make gesture settings configurable | Low |
| 3B | `PlayerSettingsScreen` + button show/hide, auto-hide timer, seek bar options | Medium |
| 3C | Subtitle auto-detect from folder, timing offset slider, font size/color panel | Medium |
| 3D | Cinematic mode overlay (lock button → full blackout, gestures work) | Medium |
| 3E | Audio delay slider, volume boost, 10-band EQ widget + presets | Medium |
| 3F | A-B loop, skip silence, frame-by-frame, chapter markers, seek thumbnail, screenshot | High |

### Files to create
- `raddflix_flutter/lib/core/player/player_prefs.dart`
- `raddflix_flutter/lib/core/player/player_prefs_provider.dart`
- `raddflix_flutter/lib/screens/player_settings_screen.dart`
- `raddflix_flutter/lib/widgets/player/cinematic_overlay.dart`
- `raddflix_flutter/lib/widgets/player/subtitle_overlay.dart`
- `raddflix_flutter/lib/widgets/player/eq_panel.dart`
- `raddflix_flutter/lib/widgets/player/ab_loop_panel.dart`
- `raddflix_flutter/lib/widgets/player/video_enhance_panel.dart`

### Files to modify
- `raddflix_flutter/lib/screens/player_screen.dart` — read prefs, pass to gesture layer, add new button slots
- `raddflix_flutter/pubspec.yaml` — add `gal: ^2.3.0` and `flutter_colorpicker: ^1.1.0`

### Design rules for player
- AMOLED black (#000000) + RaddFlix red (#E8002D) accent — match existing `AppColors`
- All panels slide from right (200ms, `AppCurves.standard`) — existing pattern
- All toggles use `Switch` with accent color
- Sliders use `Slider` with accent color thumb
- Bottom sheets for quick panels, full `Scaffold` page for `PlayerSettingsScreen`


## 9. Open Issues (Priority Order)

### 🔴 HIGH — Do these next
1. **Delete legacy workflow** `.github/workflows/build_apk.yml` (underscore) — dead file with wrong paths
2. **Set keystore secrets** in GitHub: `KEYSTORE_BASE64`, `KEYSTORE_PASSWORD`, `KEY_ALIAS`, `KEY_PASSWORD`
3. **Verify new APK builds** from Sessions 4-5 pass CI and produce working APK

### 🟡 MEDIUM — UX gaps
4. Phase 3: Player gestures (double-tap ±15s, swipe brightness/volume, 2× speed, skip intro)
5. Phase 4: Search screen (live results, filters, history chips)
6. Phase 5: Downloads screen (folder UI, storage bar, progress rings, delete)
7. Phase 6: Profile screen (stats, subscription countdown, theme toggle)
8. Phase 8: Subscription TID flow (plan select → payment → TID submit → poll)

### 🟢 LOW — Security / cleanup
9. Block port 8000: `sudo iptables -A INPUT -p tcp --dport 8000 -j DROP` on Oracle
10. Move `JWT_SECRET` + `SMS_KEY` from SQLite to server env vars
11. Upload `db_update.json` to JazzDrive CDN → update `jazzDriveDbUpdateUrl` to zero-rated URL

---

## 10. Agent Rules (Non-Negotiable)

1. **Read before touching** — always fetch file content + SHA before writing
2. **Verify after every change** — SSH confirm, or check commit SHA returned
3. **Append to TASK_LOG** after every session (Rule 8 in SKILLS.md)
4. **Never rename** `/opt/jazzmax/` or any server folder
5. **Never delete** `/opt/jazzmax/radd-hub/hub/_legacy/`
6. **Never force-push** to GitHub
7. **Package ID is always** `com.raddflix.app`
8. **JazzDrive = video only**, never images
9. **SSH key decode** — strip embedded spaces before base64 decode (see quickstart above)
10. **Update THIS file** at end of each session with current state

---

## 11. File Map Quick Reference

```
repo root/
├── raddflix_config.json              # remote config (api_base_url)
├── raddflix_flutter/                 # Flutter app (USE THIS — not root lib/)
│   ├── lib/core/constants.dart       # all app constants
│   ├── android/app/
│   │   ├── build.gradle              # package com.raddflix.app, minifyEnabled true
│   │   ├── proguard-rules.pro        # keep class com.raddflix.app.** ✅ fixed
│   │   └── keystore/raddflix.keystore
├── .github/workflows/
│   ├── build-apk.yml                 # ✅ ACTIVE — use this
│   ├── ci-tests.yml                  # ✅ ACTIVE — flutter analyze + api health
│   └── build_apk.yml                 # ❌ LEGACY — DELETE THIS
├── _watch_prototype/                 # Oracle mobile API source
│   └── routes/app_catalog.py        # catalog endpoints incl. /db_update
├── radd-hub/                         # Oracle admin backend source
├── agent-hub/
│   ├── HANDOFF_2026_05_26.md        # THIS FILE — update each session
│   ├── SKILLS.md                    # agent rules
│   ├── SETUP.md                     # environment setup
│   ├── PROMPT.md                    # starter prompt template
│   └── history/TASK_LOG.md          # session log (append after each session)
```
