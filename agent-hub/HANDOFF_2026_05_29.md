# RaddFlix Agent Handoff — 2026-05-29

## What Was Done This Session

### Task: Add IMDB + YouTube + Google Metadata Fallbacks

Added a full **6-tier metadata enrichment fallback chain** to all four enrichment-related files.

---

## Fallback Chain — Full 6 Tiers

| # | Source | Key | Purpose |
|---|--------|-----|---------|
| 1 | TMDB | vault `tmdb` (2 keys set) | International movies + TV, best quality |
| 2 | OMDB | vault `omdb` (2 keys set) | IMDB ratings, older Western content |
| 3 | AI (Groq/Gemini/OpenAI/OpenRouter) | vault `groq`/`gemini`/etc | Regional content (Pakistani/Indian) |
| 4 | IMDbAPI.dev | **none needed** | Free IMDB, Punjabi/Lollywood/South Asian |
| 5 | YouTube | vault `youtube` (optional) | Trailer thumbnail as poster |
| 6 | Google KG | vault `google` (optional) | Knowledge Graph: name/description/poster |

---

## Files Changed

### `radd-hub/hub/metadata_lookup.py`
- Added `_imdbapi_search()` — free IMDB search via imdbapi.dev, no key
- Added `_youtube_search()` — YouTube Data API v3 + HTML scrape fallback
- Added `_google_search()` — Google Knowledge Graph API
- Updated `enrich()` to call all 3 after the AI fallback (tiers 4→5→6)
- Updated `has_any_key()` docstring — now always returns True (IMDbAPI works without key)
- **Before**: 3 tiers (TMDB→OMDB→AI) | **After**: 6 tiers

### `radd-hub/hub/metadata.py`
- Added `fetch_google_kg()` function
- Updated `enrich_title()` to add Google KG as step 6 (after YouTube)
- Updated module docstring to list all 6 sources
- **Before**: 5 tiers | **After**: 6 tiers

### `radd-hub/hub/organizer.py`
- Added lazy `_get_metadata_lookup()` helper
- Added `enrich_title_metadata(title, year, media_type)` — public helper to enrich a single title using the full 6-tier chain, callable from any route/worker
- Added enrichment step at end of `auto_organize()` — enriches up to 10 low-confidence titles (confidence < 40) after organizing

### `radd-hub/hub/downloader.py`
- Added lazy `_get_metadata_lookup()` helper
- After every successful download + JazzDrive upload, calls `metadata_lookup.enrich()` for the downloaded title using `clean_name` and `year_hint`
- Saves enriched metadata to DB if title already exists

---

## Why the Scan Was Showing 18 tmdb_miss Warnings

**Root cause confirmed**: The TMDB API keys existed in the Settings tab but were NOT loaded into the key vault (`keys` table) when the scan ran. Result: `_tmdb_keys()` returned `[]`, no HTTP calls made, all 18 titles missed in under 5 seconds.

**Fix**: User confirmed keys are now set in the vault. Future scans should succeed for ~13/18 titles. The remaining 5 (Pakistani/Lollywood titles not on TMDB) will now hit IMDbAPI.dev (tier 4) or AI (tier 3) instead of failing silently.

---

## Optional Next Steps

1. Add `youtube` key to vault (Google Cloud → YouTube Data API v3 → free 10,000 req/day)
2. Add `google` key to vault (Google Cloud → Knowledge Graph Search API → free tier)
3. Run a re-scan — should see `tmdb_ok` for major titles, `imdbapi` for Pakistani films
4. Server-side watch position sync (`/watchpos` API + Flutter sync)

---

## Oracle Server
- IP: `92.4.95.252`, user: `ubuntu`
- SSH key in `ORACLE_SSH_KEY`
- Files live at `/opt/jazzmax/radd-hub/hub/`
- Services: `jazzmax_radd` (port 5000), `jazzmax_watch` (port 6000)
- **Must restart `jazzmax_radd` after deploying Python changes**

## AppConstants
- `supportWhatsApp = '923XXXXXXXXX'` — still placeholder, needs real number before release

## GitHub Push Method
```js
const vars = await viewEnvVars();
const token = vars.envVars.shared['GITHUB_TOKEN'];
// 6-step tree commit via fetch() — never bash curl for GitHub API
```
